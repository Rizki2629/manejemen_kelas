# Apache configuration for CodeIgniter 4 on Heroku
# This file is required for proper routing

# Set DirectoryIndex to use index.php (CodeIgniter front controller)
DirectoryIndex index.php

# Ensure mod_rewrite works properly
<IfModule mod_rewrite.c>
    RewriteEngine On
</IfModule>
